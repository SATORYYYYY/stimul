# Base URL of the Django REST API (include the trailing /api).
# Local development:
VITE_API_BASE_URL=http://localhost:8000/api
# Production example (set this in the Render Static Site env vars):
# VITE_API_BASE_URL=https://stimul-backend.onrender.com/api
